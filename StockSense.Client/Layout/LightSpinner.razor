<div style="display: flex; flex-direction: column; justify-content: center; align-items: center; gap: var(--space-4);">
    <BbSpinner Size="SpinnerSize.Default" />
    <div style="display: flex; flex-direction: column; align-items: center; gap: var(--space-1);">
        <p style="color: var(--muted-foreground); font-size: var(--text-xs); text-transform: uppercase; letter-spacing: 0.1em; margin: 0;">@Description</p>
    </div>
</div>

@code {
    [Parameter] public string? Description { get; set; }
}
